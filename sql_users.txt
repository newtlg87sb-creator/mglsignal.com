-- =========================================================
-- 1. USER & PROFILE MANAGEMENT (Хэрэглэгчийн удирдлага)
-- =========================================================

-- 'User control' хүснэгт үүсгэх (Хэрэглэгчийн эрх, мэдээлэл)
CREATE TABLE IF NOT EXISTS public."User control" (
  id uuid PRIMARY KEY REFERENCES auth.users ON DELETE CASCADE,
  username text,
  email text,
  membership_type text DEFAULT 'free',
  membership_expires_at timestamptz,
  updated_at timestamptz DEFAULT now()
);

-- Хэрэглэгч бүртгүүлэхэд автоматаар профайл үүсгэх функц
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public."User control" (id, username, email, membership_type)
  VALUES (
    new.id, 
    COALESCE(new.raw_user_meta_data->>'display_name', SPLIT_PART(new.email, '@', 1)), 
    new.email, 
    'free'
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Триггерийг асаах
DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE PROCEDURE public.handle_new_user();

-- Аюулгүй байдлын тохиргоо (RLS)
ALTER TABLE public."User control" ENABLE ROW LEVEL SECURITY;

-- Хэрэглэгч зөвхөн өөрийн мэдээллийг харах эрх
DROP POLICY IF EXISTS "Allow individual read" ON public."User control";
CREATE POLICY "Allow individual read" ON public."User control" 
FOR SELECT USING (auth.uid() = id);